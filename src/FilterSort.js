const FilterProducts = (productArr, condition) => {

}

const sortProducts = (productArr, order) => {
    const prices = []
    if (order === 'ascending') {
        productArr.forEach((product) => {
            
        })
    }
}